# Optional runtime dependencies for the TUI
# `curses` is part of the standard library on Linux (ncurses)
# Uncomment or add packages as needed for UI improvements or logging
rich==13.3.0
loguru==0.7.0
